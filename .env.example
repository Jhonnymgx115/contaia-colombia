# ── Base de datos PostgreSQL (rama web / desktop) ──
# Desarrollo con docker: docker compose -f db/docker-compose.yml up -d
DATABASE_URL="postgresql://contaia:contaia_dev@localhost:5432/contaia?schema=public"

# ── Asistente IA (opcional) ──
IA_API_KEY=
IA_PROVEEDOR=openai
IA_MODELO=gpt-4o-mini
